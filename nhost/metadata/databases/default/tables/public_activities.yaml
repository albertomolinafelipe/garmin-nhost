table:
  name: activities
  schema: public
array_relationships:
  - name: activity_streams
    using:
      foreign_key_constraint_on:
        column: activity_id
        table:
          name: activity_streams
          schema: public
