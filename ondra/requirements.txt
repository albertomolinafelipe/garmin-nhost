fastapi==0.115.12
strawberry-graphql[fastapi]==0.278.0
uvicorn==0.34.2
fitdecode==0.11.0

# Phase 7 must add explicit, exact pins for BOTH garminconnect and garth;
# garth must not be accepted only as garminconnect's transitive dependency.
